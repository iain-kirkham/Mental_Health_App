## Agent skills

### Issue tracker

Issues live in GitHub Issues for `iain-kirkham/Mental_Health_App`, using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context — `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
